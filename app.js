Promise.all([1,2,3,4,5,6].map((n)=>fetch(`app.part${n}.b64`).then((r)=>r.text()))).then((parts)=>{const b64=parts.join('').replace(/\s/g,'');const bin=atob(b64);const bytes=Uint8Array.from(bin,(c)=>c.charCodeAt(0));const source=new TextDecoder('utf-8').decode(bytes);(0,eval)(source);}).catch((error)=>{document.querySelector('#app').innerHTML=`<div class="game-frame"><section class="screen"><h1 class="title">DUNK ARCHIVE</h1><div class="terminal">LOAD ERROR\n${String(error)}</div></section></div>`;});
